# {{BRAND}} — Agent Instructions (cross-tool contract)

> Works in Claude Code, Codex, Cursor, Gemini CLI — any agent. `CLAUDE.md` is the deepest doctrine; this file is the laminated card. **TEMPLATE — derived by `/os-spawn`.**

## {{BRAND}} DNA

```
{{BRAND}} = {{PRODUCT}} × {{CRAFT_DIMENSION}} × {{HONESTY_DIMENSION}}
```

**Market:** {{MARKETS}}. **Voice:** {{THREE_ADJECTIVES}}.
**Test:** {{THE_SKEPTICAL_CUSTOMER_TEST — e.g. "Would a skeptical lifter who saunas twice a week trust this sentence?"}}

## The one rule that outranks everything

**No {{REGULATED_TERRITORY}} claims. Ever.** Source of truth: `lib/voice/brand-voice.ts`. Gate: `.claude/agents/claims-guard.md`. A claims FAIL blocks publish until a human rewrites.

## Before you work

1. Read `design.md` + `taste.md` before any visual work
2. Read `lib/voice/brand-voice.ts` before any copy work
3. Check `docs/intelligence/MEMORY.md` before any business/strategy work
4. Run `pnpm typecheck && pnpm build` before declaring code done

## Standards

1. Tokens from `design.md` only — if a value isn't there, it doesn't exist
2. Every factual claim: approved framing + citation
3. Update `docs/intelligence/` after significant work
4. {{ONE_OR_TWO_BRAND_SPECIFIC_STANDARDS}}

## Stop and ask a human before

Production deploys · sending email · posting to social · pricing changes · anything externally visible or irreversible.
